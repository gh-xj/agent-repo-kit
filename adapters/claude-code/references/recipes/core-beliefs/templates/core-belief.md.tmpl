# Core Beliefs

This is the philosophy of `<project>`:

1. <Imperative + one supporting clause>.

2. <...>

3. <...>

4. <...>

5. <...>

6. <...>

7. <...>

8. <...>

9. <...>

10. <...>

`<project>` will not become <category-1>, <category-2>, <category-3>, or <category-4>. Each would invert one of the rules above.

These rules are deliberately few. When a change cannot be squared with one of them, do not amend the rule — name the conflict, and decide which gives.

*Companion docs: [`invariants.md`](invariants.md) for verifiable hard constraints, [`anti-patterns.md`](anti-patterns.md) for explicit refusals.*

<!--
Template usage (delete this block before commit):
- Aim for a small, load-bearing set of rules. The exact count is the
  project's call — fewer if the project is small, more if the surface
  is large.
- Each rule: imperative verb + one supporting clause. Avoid
  multi-paragraph rules.
- The refusals paragraph names categories (service, orchestrator,
  workflow engine), not individual features.
- Form is the project's choice (Unix-philosophy imperatives, numbered
  propositions, flowing essay). Pick what fits the project's voice.
- See `../bootstrap.md` Pass 2 for the elicitation procedure.
-->
