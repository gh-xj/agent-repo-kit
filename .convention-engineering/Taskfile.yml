version: "3"

includes:
  tickets:
    taskfile: ../.tickets/Taskfile.yml
    dir: ../.tickets
  wiki:
    taskfile: ../.wiki/Taskfile.yml
    dir: ../.wiki

tasks:
  check:conventions:
    desc: Validate the tracked convention contract
    cmds:
      - bash check.sh --repo-root .. --config .convention-engineering.json

  verify:
    desc: Run the repo convention verification gates
    cmds:
      - task: tickets:test
      - task: wiki:lint
      - task: check:conventions

# Generated by agent-repo-kit --init
