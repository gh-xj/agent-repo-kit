# skill-builder

Bootstrap and audit skill routers, references, and skill-local CLI surfaces.

## Commands

- `version`: print build metadata
- `init`: scaffold a `SKILL.md` router
- `audit`: check frontmatter, router size, and referenced relative paths

## Verification

- `task ci`: canonical CI command
- `task verify`: local aggregate verification
