{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/gh-xj/agent-repo-kit/main/skills/convention-engineering/schemas/conventions.schema.json",
  "title": "Conventions Descriptor",
  "description": "Repo-root .conventions.yaml describing which conventions a repo opts into. Schema doc: skills/convention-engineering/references/core/conventions-yaml.md.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "agent_docs": {
      "description": "Agent contract files (e.g. CLAUDE.md, AGENTS.md) the repo guarantees exist with the required sections.",
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "docs_root": {
      "description": "Top-level docs directory (commonly 'docs' or '.docs'). Must contain requests/, planning/, plans/, implementation/, taxonomy/.",
      "type": "string",
      "minLength": 1
    },
    "taskfile": {
      "description": "Repo declares a canonical Taskfile.yml with a verify target.",
      "type": "boolean"
    },
    "pre_commit": {
      "description": "Repo runs a pre-commit hook (.githooks/pre-commit + core.hooksPath, or equivalent installed hook).",
      "type": "boolean"
    },
    "skill_roots": {
      "description": "Repo-local agent-skill discovery roots (e.g. .claude/skills, .agents/skills, .codex/skills).",
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "operations": {
      "description": "Adopted operational conventions. Recommended values: 'work' (.work/ tracker, see references/recipes/work/pattern.md), 'wiki' (.wiki/ knowledge base, see references/recipes/wiki/pattern.md). Other values are permitted for project-local recipes.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "min_work_version": {
      "description": "DEPRECATED in 2.0.0. Use a free-form `checks:` entry or a project-specific block instead. Minimum semver version of the external work CLI this repo's conventions assume; still probed by scripts/verify.sh for backward compatibility.",
      "deprecated": true,
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$"
    },
    "checks": {
      "description": "Free-form list of agent-readable rules. Interpreted by the convention-engineering audit and evaluation rubric (references/meta/evaluation-rubric.md); not machine-validated by this schema.",
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "epic": {
      "description": "Epic-wrapper pattern configuration. Leaves are sibling repo names; scripts/bootstrap.sh links each one under repo/<leaf>.",
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "leaves": {
          "description": "Sibling repo directory names. Each leaf implies a sibling checkout at ../<leaf> and a wrapper-local symlink at repo/<leaf>.",
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true
        },
        "composed": {
          "description": "When true, the repo may assert compose.yaml lists each leaf.",
          "type": "boolean"
        }
      }
    },
    "core_beliefs": {
      "description": "Core-beliefs trio adoption (core-belief.md + invariants.md + anti-patterns.md). See references/patterns/core-beliefs.md.",
      "oneOf": [
        { "type": "boolean" },
        {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "description": "Whether the trio is adopted.",
              "type": "boolean"
            },
            "path": {
              "description": "Directory containing the trio. Defaults to <docs_root>.",
              "type": "string",
              "minLength": 1
            }
          }
        }
      ]
    }
  }
}
